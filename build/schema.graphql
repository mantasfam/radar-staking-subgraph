type Transaction @entity(immutable: true) {
  id: Bytes!
  user: User!
  amount: BigInt!
  stakedFlg: Boolean!
  withdrawnFlg: Boolean!
}

type User @entity {
  id: Bytes!
  user: Bytes!
  amountStaked: BigInt!
  transactions: [Transaction!]! @derivedFrom(field: "user")
}
